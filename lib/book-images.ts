/**
 * /book hero — `null` shows gradient placeholder; otherwise a path under `public/`.
 */
export const bookImages: { hero: string | null } = {
  hero: '/images/book/hero.png',
}

export const BOOK_HERO_PATH = '/images/book/hero.png' as const
